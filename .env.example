# Server Configuration
PORT=3000
NODE_ENV=development

# Email Configuration (using Gmail as example)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# Admin Email (where messages will be sent)
ADMIN_EMAIL=logan@example.com

# MongoDB (optional - for storing messages)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/resume-messages?retryWrites=true&w=majority

# Site Configuration
SITE_URL=http://localhost:3000
